# Test: encoding.decode_json - decode a JSON string and print the result
# Validates: Requirement 4.8

import "encoding"
import "fmt"

$json = "{\"hello\": 42}"

$out = encoding.decode_json($json, "map{str: int}")

print "json" {
	msg => fmt.printf("json: %v", $out),
}
